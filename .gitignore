# WhatsApp session credentials. Never commit these: they are a full credential
# set that can read and send as the bot account from anywhere.
store/
*.db
*.db-shm
*.db-wal
.bridge-token

# Local config generated by install.sh
.env
env.sh

# askwatch dedupe cursor, when left at its default location
.askwatch-seen.json
.askwatch-seen.json.tmp

__pycache__/
*.pyc
.DS_Store
*.bak
